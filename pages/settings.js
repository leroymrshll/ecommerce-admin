import Layout from "@/components/Layout";

export default function Settings() {
    return (
        <Layout>
            <h1>
                This is the settings page.
            </h1>
        </Layout>
    );
}